# Override do systemd para o Caddy carregar variáveis do projeto
#
# Instalação (rode no servidor como root):
#   sudo mkdir -p /etc/systemd/system/caddy.service.d/
#   sudo cp ci/caddy-override.conf /etc/systemd/system/caddy.service.d/override.conf
#   sudo systemctl daemon-reload
#   sudo systemctl restart caddy
#
# Ajuste o caminho abaixo para o diretório real do projeto.

[Service]
# Carrega apenas as variáveis que o Caddy precisa (geradas por ci/caddy-env.sh)
EnvironmentFile=/etc/caddy/sweflow.env
ExecStart=
ExecStart=/usr/bin/caddy run --environ --config /root/api_sweflow_php/Caddyfile --adapter caddyfile
ExecReload=
ExecReload=/usr/bin/caddy reload --config /root/api_sweflow_php/Caddyfile --adapter caddyfile --force
